/// Public entrypoint for the `mvc_swagger_codegen` package.
///
/// This library is intentionally minimal because the package is primarily a CLI
/// generator, but the documented constant below gives pub.dev a stable public
/// API surface to score.
library mvc_swagger_codegen;

/// Current package version.
const String packageVersion = '0.0.6';

